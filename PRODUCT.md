# BitVault — BTC 量化交易操作系统 · 产品需求文档（PRD）

| 项 | 内容 |
| --- | --- |
| 产品名称 | BitVault（BTC 量化交易操作系统） |
| 版本 | PRD v1.2（2026-08-31，经资金安全评审修订） |
| 文档状态 | 待评审 |
| 关联交易所 | 欧易 OKX（API v5，REST + WebSocket） |
| 目标平台 | Web UI（浏览器访问，本地部署） |

---

## 1. 产品概述

### 1.1 一句话定位

BitVault 是一套**本地部署、带专业 Web UI 的 BTC 量化交易操作系统**：通过欧易 OKX API v5 接入行情与交易，提供「行情监控 → 策略编写 → 历史回测 → 模拟盘验证 → 实盘执行 → 风控监控」的完整闭环，对标大厂量化交易终端（OKX Web 交易台 / Freqtrade / Hummingbot / VN.PY）的核心能力，但面向个人与小团队，开箱即用。

### 1.2 设计原则（参考大厂系统的共性）

大厂加密货币量化系统的核心拆解，本产品逐一对应：

| 大厂系统能力 | 代表系统 | BitVault 对应模块 |
| --- | --- | --- |
| 行情统一网关（多市场、多周期） | VN.PY / CCXT | **行情数据中心**（M1） |
| 策略引擎与生命周期管理 | Freqtrade / Hummingbot | **策略中心**（M3） |
| 历史回测 + 绩效归因 | Freqtrade / QuantConnect | **回测系统**（M4） |
| OMS 订单全生命周期管理 | 交易所 Web 台 / 3Commas | **实盘执行引擎**（M5） |
| 事前/事中/事后三层风控 | 机构级做市系统 | **风控中心**（M6） |
| 监控、告警、审计日志 | 全部 | **监控与日志**（M7） |
| 账户与资产全景 | OKX 资产页 | **账户与资产**（M2） |
| 交易终端 UI（K 线 + 盘口 + 下单） | OKX 网页交易台 | **交易台页面**（UI-P3） |

### 1.3 目标用户

- **主要用户**：有一定量化经验的个人交易者 / 小团队（1~5 人），希望 7×24 无人值守运行策略。
- **次要用户**：程序化交易学习者，通过策略模板 + 回测系统学习量化流程。

### 1.4 核心价值主张

1. **闭环**：从策略 idea 到实盘，不离开本系统。
2. **安全**：API Key 本地加密存储、只开「交易」权限、默认接 OKX 模拟盘。
3. **可控**：三层风控 + 一键停止（Kill Switch），任何时刻可人工接管。
4. **可观测**：策略运行状态、持仓、订单、成交、收益曲线全部实时可视化。

### 1.5 明确不在范围内（Non-Goals）

- 不做托管资金 / 代客理财 / 社交跟单社区。
- 不做高频做市（tick 级延迟竞争），目标延迟级别为秒级~毫秒级。
- 不做中心化托管资产，资金始终留在用户自己的 OKX 账户。
- MVP 阶段只做 **BTC 相关现货与 U 本位永续合约**，不铺多币种。

---

## 2. 核心用户旅程

```
配置 API Key → 行情验证连通 → 编写/选择策略 → 回测调参
     → 模拟盘跑 7 天 → 风控参数确认 → 实盘小仓位启动
     → 日常监控 Dashboard → 异常自动止损/告警 → 周期性复盘
```

关键用户故事（User Stories）：

1. 作为交易者，我想要**输入 OKX API Key 后一键看到**我的账户总览（权益、可用、持仓、未实现盈亏），以便快速决策。
2. 作为交易者，我想要**用内置策略模板**（网格、双均线、布林带、资金费率套利）填参数就能跑，而不必从零写代码。
3. 作为进阶用户，我想要**用 Python 写策略脚本**并通过回测验证后，一键推送到模拟盘/实盘。
4. 作为交易者，我想要**设置全局风控**（最大回撤、单笔最大仓位、日内亏损熔断），触发后自动平仓并停止策略。
5. 作为交易者，我想要**手机浏览器打开 Dashboard** 也能看到当前持仓与收益曲线，收到异常告警通知。

---

## 3. 功能需求（按模块，含优先级）

> 优先级：P0 = MVP 必须；P1 = V1.0 必须；P2 = 后续版本。

### M1 行情数据中心（P0）

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| M1-1 | K 线订阅 | BTC-USDT 现货 + BTC-USDT-SWAP 永续；周期 1m/5m/15m/1H/4H/1D；通过 OKX WS `candle{period}` 频道实时推送，REST `/api/v5/market/candles` 补历史 |
| M1-2 | 实时盘口与成交 | WS `books5`（5 档盘口）+ `trades`（逐笔成交），用于交易台与策略信号 |
| M1-3 | Ticker 概览 | 最新价、24h 涨跌幅、24h 成交量、资金费率（SWAP）、标记价格 |
| M1-4 | 行情落地 | K 线/成交写入本地数据库，供回测使用；支持一键补齐指定时间范围的历史 K 线 |
| M1-5 | 连接状态 | 行情网关连接状态、断线自动重连（指数退避）、数据心跳检测 |
| M1-6 | 多周期指标缓存 | 后台计算并缓存常用指标（MA/EMA/BB/RSI/ATR/资金费率均值），策略与 UI 直接取用 |
| M1-7 | bar 收盘确认 | WS 推送的当根 K 线是**未收盘**的，仅对已收盘 bar 触发策略 on_bar（按交易所 bar 时间戳判定），杜绝重复/提前信号 |
| M1-8 | 深历史下载 | `/market/candles` 仅返回最近 1440 根；更早历史经 `/market/history-candles` 分页拉取（限频更低），带下载进度与断点续传；K 线按 UTC 对齐落库 |

### M2 账户与资产（P0）

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| M2-1 | API Key 管理 | 录入 OKX API Key / Secret / Passphrase；AES-256-GCM 加密落盘；支持「模拟盘 / 实盘」两套配置；支持只读 Key（仅查询不下单） |
| M2-2 | 资产总览 | 总权益（USD 折算）、可用余额、已用保证金、未实现盈亏、当日/当周盈亏；数据来自 REST `/api/v5/account/balance` + WS `account` 频道 |
| M2-3 | 持仓明细 | 方向、数量、开仓均价、标记价格、未实现盈亏、强平价、杠杆倍数；来自 `/api/v5/account/positions` |
| M2-4 | 资金曲线 | 按小时/天快照权益，绘制累计收益曲线，计算最大回撤 |
| M2-5 | 交易记录 | 成交明细、手续费（含返佣）、已实现盈亏聚合报表（日/周/月） |
| M2-6 | 账户配置检测 | 启动时读取账户模式（现货/单币种保证金/跨币种保证金）与持仓方式（净/双向），系统仅支持受检组合并给出设置引导；下单参数 tdMode/posSide 据此构造 |
| M2-7 | 保证金风险监控 | 实时展示标记价格、强平价、保证金率；距强平价 <10% 黄色预警、<5% 红色告警，可配自动减仓 |
| M2-8 | 子账户隔离（推荐） | 推荐策略交易使用独立 OKX 子账户 API Key，与手动交易物理隔离；未隔离时按订单来源（strategy/manual）本地记账归属 |

### M3 策略中心（P0：模板策略；P1：自定义策略）

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| M3-1 | 策略模板库（P0） | 内置 4 类模板：① 网格交易（现货/合约；**突破上下轨行为可配**：停止/清仓/跟随网格）② 双均线趋势（金叉死叉；**方向可选只多/多空**）③ 布林带均值回归 ④ 资金费率套利（现货+永续 delta 中性**双腿对冲**，依赖现货与合约同时操作） |
| M3-2 | 参数配置（P0） | 每个模板暴露参数面板（如网格：上下轨、格数、每格仓位；均线：快慢周期、止损比例）；参数校验 |
| M3-3 | 自定义策略（P1） | Python 策略脚本：继承统一策略基类 `BaseStrategy`，约定 `on_bar / on_tick / on_order` 回调；内置代码编辑器（语法高亮 + 保存校验） |
| M3-4 | 策略生命周期（P0） | 状态机：`草稿 → 回测通过 → 模拟盘运行 → 暂停 → 实盘运行 → 停止/异常停止`；同标的同方向同时只允许一个实盘策略（可配置） |
| M3-5 | 策略实例监控（P0） | 每个实例显示：状态、运行时长、累计收益、当前持仓、今日信号数、最近日志 |
| M3-6 | 实例状态持久化 | 网格已成交格位、指标缓存、**持仓↔实例绑定关系**落库；重启后先与交易所对账再恢复运行，对不上则告警待人工确认 |

### M4 回测系统（P0 基础版）

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| M4-1 | 回测引擎 | 基于 M1 落地的历史 K 线回放；按 bar 收盘事件驱动（与实盘同一套策略接口，保证回测/实盘一致性）；**成交假设：bar N 收盘产生信号、bar N+1 开盘价 ± 滑点成交，杜绝未来函数** |
| M4-2 | 成本模型 | 手续费（taker/maker 可配，默认现货 taker 0.1%、合约 taker 0.05%）、滑点（固定比例可配）、资金费率（合约按 8h 结算模拟）、爆仓模拟（保证金触及维持保证金率按强平处理） |
| M4-3 | 绩效指标 | 总收益率、年化、最大回撤、夏普、Sortino、胜率、盈亏比、交易次数、平均持仓时间、逐笔交易明细 |
| M4-4 | 回测报告 | 收益曲线 vs 买入持有（Buy & Hold）对比图、回撤曲线、月度收益热力表 |
| M4-5 | 参数寻优（P2） | 网格搜索/随机搜索批量回测，输出参数—收益矩阵 |

### M5 实盘执行引擎（OMS）（P0）

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| M5-1 | 订单全生命周期 | 本地状态机：`待报 → 已报 → 部分成交 → 全部成交 / 已撤销 / 失败`；以 WS `orders` 频道推送为准 + REST 定时对账兜底 |
| M5-2 | 下单封装 | 统一下单接口：市价/限价、现货/合约、开平仓方向、`post_only` / `ioc` / `fok` 标记；自动处理 OKX 最小下单量与精度（`lotSz` / `minSz`） |
| M5-3 | 幂等与防重 | 客户端自定义订单号 `clOrdId`（BUCKETS 规则），网络超时后先查询再补单，严禁重复报单 |
| M5-4 | 手动交易台（P0） | UI 手动下/撤单、一键平仓、一键撤销所有挂单（人工接管通道，与策略单用不同来源标记） |
| M5-5 | 持仓对账 | 每分钟 REST 拉取实际持仓与本地缓存比对，不一致立即告警并暂停策略 |
| M5-6 | 策略执行算法（P1） | TWAP 拆单执行大额仓位；被动挂单跟踪（追价） |
| M5-7 | 交易所侧止损（P0） | 开仓时经 `attachAlgoOrds` **附带止损止盈单**（或独立 algo 止损单），由交易所托管执行，本系统宕机/断网仍生效；本地止损仅作二级兜底 |
| M5-8 | 滑点保护 | 默认禁用裸市价单，改用**限价 IOC + ±0.2% 保护价**；单笔大额下单前先查盘口深度 |
| M5-9 | 竞态与精度 | 撤单与成交竞态以交易所终态为准（OKX 状态含 `partially_canceled`）；数量按 lotSz/minSz **向下取整**，合约按 ctVal 换算张数 |

### M6 风控中心（P0 — 大厂系统的灵魂，优先级最高之一）

三层风控架构：

| 层 | 编号 | 需求 | 说明 |
| --- | --- | --- | --- |
| 事前（下单前拦截） | M6-1 | 订单合法性 | 单笔金额上限、委托价格偏离标记价格 ±N% 拒绝、下单频率上限（如 10 单/秒） |
| 事前 | M6-2 | 仓位限额 | 单策略最大仓位（币量 / 权益百分比）、账户总杠杆上限 |
| 事中（持仓中监控） | M6-3 | 策略级止损 | 策略止损/止盈、移动止损（按 ATR 或百分比）；**优先通过交易所附带止损单实现（见 M5-7）**，本地触发兜底 |
| 事中 | M6-4 | 账户级熔断 | 日内亏损达 X%（默认 3%）→ 撤销所有挂单 + 全部减仓 + 停止所有策略 + 强告警；账户最大回撤达 Y% → 同上 |
| 事中 | M6-5 | 系统级保护 | 交易所 API 连续报错（如限频/签名失败）N 次 → 停止新开仓，保留人工通道 |
| 事后 | M6-6 | 一键 Kill Switch | 全局按钮：撤销全部挂单 + 市价全平 + 所有策略停机；需二次确认；单步失败**自动重试 3 次**并输出执行结果清单 |
| 事后 | M6-7 | 风控事件日志 | 所有拦截/熔断动作落库并可查，带触发原因快照 |
| 事后 | M6-8 | 风控状态持久化 | 熔断状态、日内亏损计数**落盘，进程重启不重置**（不可借重启绕过熔断）；「日内」按 UTC 0 点重置 |
| 事前 | M6-9 | 逐仓默认 | 合约策略默认 isolated **逐仓**（可配），单仓爆仓风险不蔓延至全账户 |

### M7 监控、告警与日志（P1）

| 编号 | 需求 | 说明 |
| --- | --- | --- |
| M7-1 | 系统监控页 | 行情/私有 WS 连接状态、延迟、内存/CPU、队列积压、策略心跳 |
| M7-2 | 告警通道 | Web UI 通知中心（P0）+ Telegram / 邮件 / Bark Webhook（P1，可配） |
| M7-3 | 告警事件 | 策略异常停止、风控熔断、持仓对账不一致、WS 断线超时、订单被交易所拒绝 |
| M7-4 | 审计日志 | 所有下单/撤单/风控动作/参数修改，记录 操作来源（策略实例/人工/API）、时间、参数快照，不可篡改追加写入 |
| M7-5 | 策略日志 | 每个策略实例独立日志流，UI 内实时查看（类似 `docker logs`） |
| M7-6 | 告警风暴抑制 | 同类事件设 5 分钟冷却窗口并聚合推送（如「WS 断线 N 次」合并为一条），防告警轰炸 |

### M8 系统设置（P0 最小集）

- 交易所环境切换：**OKX 模拟盘（Demo Trading）/ 实盘**（切换需二次确认，顶部常驻红/绿环境标识）。
- 交易对与杠杆设置（合约策略启动前强制确认杠杆）。
- 模拟盘引导：OKX 模拟盘虚拟资金需在网页端 Demo Trading 页面领取；系统检测到模拟账户余额为 0 时在 UI 给出指引。
- 数据保留策略（K 线、日志自动清理）。
- 告警 Webhook 配置。

---

## 4. UI / UX 设计

### 4.1 信息架构（左侧导航，桌面优先、兼容平板/手机浏览）

```
BitVault
├── 📊 总览 Dashboard        （默认首页）
├── 📈 行情中心              （K线 + 盘口 + 资金费率）
├── 💰 账户资产              （权益/持仓/资金曲线/成交流水）
├── 🤖 策略中心              （模板列表 / 我的策略 / 新建）
│    └── 策略实例详情        （状态/持仓/日志/启停）
├── 🔬 回测中心              （新建回测 / 回测报告）
├── 📋 订单管理              （挂单/历史订单/成交明细）
├── 🛡️ 风控中心              （风控参数 / 熔断状态 / 风控事件）
├── 🔔 通知中心
└── ⚙️ 系统设置              （API Key / 交易环境 / 告警）
```

### 4.2 页面原型要点

**P1 总览 Dashboard**
- 顶部：账户权益卡（总权益、今日盈亏、未实现盈亏、可用余额）+ 当前环境标识（**绿色 DEMO / 红色 LIVE**，全站常驻）。
- 中部：BTC 实时价格迷你 K 线 + 主要策略实例状态卡片（运行中/暂停/异常 着色）。
- 下部：账户资金曲线 + 最近成交流水 + 最近风控/告警事件。

**P3 交易台（行情中心）**
- 三栏布局（参考 OKX 网页交易台）：左「K 线图（lightweight-charts，支持叠加 MA/BB）」、右「盘口 5 档 + 最近成交」、下「手动下单面板（市价/限价、数量滑杆、一键平仓）」。
- 所有手动操作有确认弹窗；LIVE 环境下下单按钮需滑动确认。

**P5 回测报告页**
- 收益曲线（策略 vs Buy&Hold）+ 回撤区间高亮 + 指标卡（夏普/最大回撤/胜率）+ 逐笔交易表（可导出 CSV）。

### 4.3 交互红线（防误操作）

1. LIVE 环境常驻红色顶栏；DEMO 常驻绿色，切换必须二次确认 + 输入确认文案。
2. Kill Switch 必须二次确认，触发后弹出执行结果清单（撤销 N 单、平仓 N 仓）。
3. 所有下单类操作 3 秒内给出成功/失败反馈，失败展示交易所原始错误码与中文释义。
4. 策略从「模拟盘」切「实盘」需展示该策略最近回测摘要 + 风控参数确认清单。

---

## 5. 系统架构设计

### 5.1 总体架构（本地部署，单机进程组）

```
┌────────────────────────────────────────────────────────────┐
│                     Web UI（浏览器）                          │
│        React 18 + TypeScript + Ant Design + lightweight-charts │
└───────────────▲────────────────────────────▲────────────────┘
                │ REST (管理/查询)            │ WebSocket (实时推送)
┌───────────────┴────────────────────────────┴────────────────┐
│                  后端服务 FastAPI（单进程内多任务）              │
│  ┌──────────┐ ┌──────────┐ ┌───────────┐ ┌───────────────┐  │
│  │ API 网关  │ │ WS 推送   │ │ 策略调度器  │ │  回测引擎      │  │
│  └────┬─────┘ └────┬─────┘ └─────┬─────┘ └──────┬────────┘  │
│       │     ┌──────┴─────────────┴───────────────┴────────┐  │
│       │     │              事件总线 (asyncio Queue)          │  │
│       │     └──┬──────────┬──────────┬──────────┬──────────┘  │
│  ┌────▼────┐ ┌─▼────────┐ ┌▼─────────┐ ┌▼────────┐ ┌───────▼─┐│
│  │行情网关  │ │交易网关   │ │ 账户服务  │ │ 风控引擎 │ │ 持久层   ││
│  │(OKX WS) │ │(OMS)     │ │(资产/持仓)│ │(三层风控)│ │SQLite   ││
│  └────┬────┘ └────┬─────┘ └────┬─────┘ └────┬────┘ └────┬────┘│
└───────┼───────────┼────────────┼────────────┼───────────┼─────┘
        ▼           ▼            ▼            ▼           ▼
   OKX 公共 WS   OKX REST+私有WS  OKX 账户API   (内存+落库)   本地文件
   wss://ws.okx.com  /api/v5/*   /api/v5/account           bitvault.db
```

### 5.2 关键设计决策

| 决策点 | 选择 | 理由 |
| --- | --- | --- |
| 事件驱动模型 | asyncio + 进程内事件总线（K线/成交/订单/账户四类事件） | 与 OKX WS 推送模型天然契合；单机场景无需引入消息队列 |
| 回测/实盘同构 | 策略只面向统一事件接口（`on_bar/on_tick/on_order`），回测用历史回放器驱动，实盘用行情网关驱动 | 保证「回测过 = 实盘行为一致」，杜绝回测实盘两套代码 |
| 风控位置 | 风控引擎作为所有订单的**强制前置管道**（策略单与手动单都经过） | 任何路径都无法绕过事前风控 |
| OKX 接入 | 自研轻封装层（直接对接 OKX v5），关键接口有单测 + 本地录像回放 | 依赖可控、便于处理签名/限频细节；必要时可平滑扩展 Binance |
| 时间同步 | 所有内部事件带交易所时间戳，本地仅作展示时区转换 | 避免时钟漂移导致对账混乱 |
| 回测隔离 | 回测引擎在**独立子进程**运行，结果经文件/队列回传 | CPU 密集回测不阻塞实盘事件循环与下单路径 |

### 5.3 核心数据流（以「策略开仓」为例）

```
行情网关(WS candle) → 事件总线 → 策略实例.on_bar() → 产生 Signal
→ 风控引擎事前检查(仓位/频率/价格偏离) → OMS 生成订单(clOrdId)
→ 交易网关 REST /api/v5/trade/order → OKX
→ OKX WS orders 推送回报 → OMS 更新状态 → 事件总线 →
   ├→ UI 实时刷新   ├→ 账户服务更新持仓   ├→ 风控引擎更新持仓占用   └→ 落库
```

---

## 6. OKX API 接入设计

### 6.1 接入范围（OKX API v5）

| 能力 | 接口/频道 | 用途 |
| --- | --- | --- |
| 历史 K 线 | `GET /api/v5/market/candles` | 回测数据、图表补齐 |
| 实时 K 线 | WS public `candle{bar}` | 策略信号 |
| 盘口/成交 | WS public `books5` / `trades` | 交易台、tick 级策略 |
| Ticker | WS public `tickers` / REST | Dashboard 行情卡 |
| 资金费率 | WS public `funding-rate` + REST | 费率套利策略 |
| 登录私有频道 | WS private（`op:login`） | 订单/账户/持仓实时推送 |
| 下单/撤单 | `POST /api/v5/trade/order`、`cancel-order`，支持 `clOrdId` | OMS 执行 |
| 批量下单（P1） | `POST /api/v5/trade/order/batch-orders` | 网格/TWAP 效率优化 |
| 订单查询/历史 | `GET /api/v5/trade/order`、`orders-history` | 对账、订单页 |
| 账户余额/持仓 | `GET /api/v5/account/balance`、`positions` | 资产页、对账 |
| 仓位与杠杆 | `POST /api/v5/account/set-leverage` | 合约策略启动时设置 |
| 交易配置 | 账户模式（现货模式/单币种保证金/跨币种保证金）在 UI 中检测并提示，不代用户改模式 | 降低误配置风险 |

### 6.2 认证与安全

- REST 头：`OK-ACCESS-KEY` / `OK-ACCESS-SIGN`（HMAC-SHA256，签名字符串 = `timestamp + method + requestPath + body`，Base64）/ `OK-ACCESS-TIMESTAMP`（ISO 8601）/ `OK-ACCESS-PASSPHRASE`。
- 模拟盘：REST 头加 `x-simulated-trading: 1`；WS 连 `wss://wspap.okx.com:8443/ws/v5/private`（公共频道与实盘同地址）。
- **时间同步**：启动及每小时与 OKX 服务器时间（`/api/v5/public/time`）校验，偏差 > 2s 记录本地偏移量并用于签名（签名时间戳容差约 ±30s，时钟漂移会导致全部请求失败）。
- Secret 与 Passphrase 使用 AES-256-GCM 加密存储，主密钥存本地密钥文件（首次启动生成），内存中解密使用、不落日志。
- 引导用户创建 API Key 时：**只勾选「交易」，不勾选「提币」**，并建议绑定服务器 IP 白名单；设置页对只读 Key 提供「仅查询模式」。

### 6.3 限频与稳定性

- 封装层内置令牌桶限频（按 OKX 各端点限额保守取 60%），429/50011 自动退避重试。
- WS 心跳：30s 发送 `ping`；断线指数退避重连（1s→2s→…→60s 封顶），重连后自动重登录私有频道并补拉 REST 快照对账。
- 订单回报双通道：WS `orders` 为主，REST 每 60s 全量对账为兜底；本地状态与交易所不一致 → 告警 + 暂停相关策略。

---

## 7. 数据模型（核心表，SQLite 起步，字段可迁移 PostgreSQL）

| 表 | 关键字段 |
| --- | --- |
| `api_keys` | name, env(demo/live), key_enc, secret_enc, passphrase_enc, permission(readonly/trade), created_at |
| `bars` | inst_id, period, open_time, o/h/l/c, vol, vol_ccy, source(rest/ws) —— 唯一键 (inst_id, period, open_time) |
| `strategies` | id, name, type(grid/ma_cross/boll/funding_arb/custom), code_path, params_json, status, inst_id, mode(backtest/demo/live) |
| `strategy_instances` | id, strategy_id, status, started_at, stopped_at, pnl, position_cache_json |
| `signals` | id, instance_id, ts, action(open/close/adjust), reason, payload_json |
| `orders` | cl_ord_id(唯一), ord_id(交易所), instance_id, inst_id, side, pos_side, type, price, sz, state, filled_sz, avg_px, fee, source(strategy/manual), created_at |
| `trades` | ord_id, trade_id, px, sz, fee, ts |
| `account_snapshots` | ts, equity, available, margin_used, unrealized_pnl —— 用于资金曲线 |
| `backtests` | id, strategy_id, params_json, start, end, fee_rate, slippage, metrics_json, report_path, created_at |
| `risk_rules` | type, scope(global/strategy_id), params_json, enabled |
| `risk_events` | ts, rule_type, scope, action(blocked/liquidated/halted), detail_json |
| `audit_logs` | ts, actor(strategy:{id}/user/api), action, payload_json, result |
| `notifications` | ts, level, channel, title, body, read |

---

## 8. 技术选型

| 层 | 选型 | 说明 |
| --- | --- | --- |
| 前端 | React 18 + TypeScript + Vite + Ant Design 5 + lightweight-charts + Zustand | AntV/ECharts 兜底复杂图表；UI 中文界面 |
| 后端 | Python 3.11~3.12（≥3.11，本机 3.12 即可）/ FastAPI / uvicorn / aiohttp(WS) / pydantic | 单进程 asyncio，组件化包结构 |
| 数据库 | SQLite(WAL) → 可迁 PostgreSQL | 个人部署零运维；K 线表建组合索引 |
| 加密 | cryptography(AES-256-GCM) | API Key 落盘加密 |
| 部署 | 一键脚本启动前后端；支持 Windows / Linux / Docker | 本地优先 |
| 测试 | pytest + OKX 响应录像回放（respx/vcr 风格）+ 回测引擎与实盘事件同构验证 | 关键路径：签名、对账、风控管道 |

---

## 9. 非功能需求

| 维度 | 指标 |
| --- | --- |
| 性能 | tick→策略信号→报单端到端 P99 ≤ 500ms（本机网络正常）；UI 行情刷新 ≤ 1s；回测 1 年 1m K 线 ≤ 60s |
| 可用性 | 7×24 连续运行；行情 WS 断线 30s 内自动恢复；进程崩溃自动重启（ supervisor/PM2/计划任务任选其一文档化） |
| 安全 | Secret 不落明文日志；API Key 加密主密钥可选由用户主口令 PBKDF2 派生（整机被拷贝时密钥仍不可用）；UI 本地监听默认 `127.0.0.1`，远程访问需配置访问口令；所有危险操作审计留痕 |
| 可靠性 | 订单路径幂等（clOrdId 去重）；进程重启后自动恢复策略实例状态与持仓缓存（先对账再恢复运行） |
| 兼容 | Chrome/Edge/Safari 最新两个大版本；响应式适配 ≥ 1280px 桌面与 ≥ 768px 平板 |

---

## 10. 资金安全设计（红线白名单）

> 本系统将直接操作用户的 OKX 账户（现货 + U 本位永续合约 + 模拟盘），资金安全为一票否决项。以下红线为系统行为白名单，任何功能不得越线。

### 10.1 系统永远不会做的事

| # | 红线 | 实现机制 |
| --- | --- | --- |
| R1 | **永不提币、永不划转** | API Key 权限引导只勾「交易」；代码层面不调用任何资金划转/提币接口 |
| R2 | **永不使用裸市价单** | 所有市价意图经「限价 IOC + ±0.2% 保护价」执行（M5-8） |
| R3 | **永不下单到未授权标的** | 交易标的白名单（默认仅 BTC-USDT / BTC-USDT-SWAP），其他 instId 一律拒单 |
| R4 | **永不绕过风控报单** | 策略单与手动单共用唯一前置风控管道，代码架构上无第二条下单路径 |
| R5 | **永不静默改杠杆/保证金模式** | 设置杠杆需 UI 显式确认并留审计；不代用户修改账户模式（M2-6） |
| R6 | **永不让止损只存在于本地** | 合约持仓必须有交易所侧止损单（M5-7），本地止损仅兜底 |
| R7 | **永不自动恢复实盘策略** | 进程重启后实盘策略一律进入「待人工确认」状态，对账通过且人工确认后才恢复 |
| R8 | **密钥永不落明文** | AES-256-GCM 加密 + 可选主口令派生；日志/错误信息统一脱敏 |

### 10.2 交易环境与资金隔离

1. **三环境支持**：模拟盘（Demo，默认）/ 实盘；模拟盘用真实行情 + 虚拟资金，通过 `x-simulated-trading: 1` 隔离，实盘代码路径与模拟盘**完全一致**，仅环境标识与 Key 不同——杜绝「模拟一套代码、实盘另一套」。
2. **实盘准入闸门**：模拟盘运行 ≥7 天且回测通过 → 才解锁实盘按钮；实盘启动强制二次确认 + 首单仓位 ≤ 最小仓位档。
3. **仓位隔离**：推荐 OKX 子账户隔离策略仓与手动仓；单账户时按订单来源记账，Kill Switch 执行前展示将影响的持仓清单。
4. **逐仓默认**（M6-9）+ 杠杆上限（默认 ≤5x，可调）。

### 10.3 亏损的最后一道闸

- 三层风控（事前拦截 / 事中熔断 / 事后审计，M6）+ 风控状态持久化（重启不可绕过）。
- 交易所侧止损单保底（M5-7）；保证金率 <5% 红色告警 + 可配自动减仓（M2-7）。
- 全局 Kill Switch：撤全部挂单 + 全平 + 停机，失败自动重试并输出清单（M6-6）。
- 每分钟持仓对账，不一致即暂停策略（M5-5）；所有动作审计留痕（M7-4）。

## 11. 风险与合规声明

1. **市场风险**：量化策略不保证盈利，产品在回测报告与实盘启动页固定展示风险提示（历史回测不代表未来收益）。
2. **技术风险**：交易所 API 故障、网络中断可能导致策略行为异常——以风控熔断 + 人工 Kill Switch 兜底。
3. **合规**：本产品为本地工具软件，不托管用户资产、不提供投资建议；用户需自行确保其所在司法辖区允许使用交易所 API 交易。API Key 权限最小化（禁提币）为强制引导。

---

## 12. 版本路线图

| 版本 | 范围 | 验收标准 |
| --- | --- | --- |
| **MVP（V0.1）** | M1 行情 + M2 账户(模拟盘) + M3 模板策略(双均线/网格) + M4 基础回测 + M5 下单/撤单/对账 + M6 三层风控核心 + M8 设置 + Dashboard/交易台/策略/回测四页 | OKX 模拟盘全流程跑通：配 Key → 回测双均线 → 模拟盘自动开平仓 → 风控熔断触发有效 |
| **V1.0** | M3 自定义策略 + M5 手动交易台完善 + M7 监控告警(TG/邮件) + 资金曲线与报表 + LIVE 实盘开关 | 实盘小额(≤100 USDT)稳定运行 7 天无对账差异；断电重启自动恢复 |
| **V1.5** | 布林带/资金费率套利模板、TWAP、参数寻优、CSV 导出、移动端适配 | 4 类模板均可回测+实盘；告警触达 ≤ 30s |
| **V2.0** | 多币种(BTC/ETH 为主)、多交易所适配层(Binance)、插件化策略市场 | 交易所适配接口抽象完成，新增交易所 ≤ 2 周 |

---

## 13. 术语表

| 术语 | 释义 |
| --- | --- |
| OMS | Order Management System，订单管理系统，负责订单状态机与交易所交互 |
| clOrdId | 客户端自定义订单号，用于幂等去重 |
| 模拟盘（Demo Trading） | OKX 提供的仿真交易环境，资金为虚拟，行情真实 |
| Kill Switch | 一键撤销全部挂单并平掉全部仓位的安全机制 |
| Buy & Hold | 买入持有基准，回测对比参照 |
| 资金费率 | 永续合约多空双方定期支付的费用，套利策略核心因子 |
| 三层风控 | 事前拦截（下单前）/ 事中监控（持仓与账户级熔断）/ 事后审计 |

---

*本文档为 V1.0 基线，后续按「评审意见 → 修订 → 冻结」流程维护；开发以 MVP 范围为唯一先期目标。*
