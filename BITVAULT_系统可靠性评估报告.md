# BitVault 量化交易系统 · 可靠性评估报告

> 评估日期：2026-09-09
> 评估方式：全量通读后端约 1.2 万行 Python 源码（交易链路逐行）、运行测试套件（56 用例全部通过）、
> 阅读三份内部文档（README / PRODUCT.md / BITVAULT_STRATEGY_RESEARCH.md），
> 并对照市面成熟开源/商业加密货币量化系统（freqtrade、Hummingbot、Nautilus Trader、Jesse、3Commas 类产品）的通行实践做差距分析。
> 本报告只做工程评估，不构成投资建议。

---

## 一、总体结论（TL;DR）

**BitVault 是一个工程质量明显高于个人项目平均水平、安全意识与研究纪律出色的单交易所趋势跟随系统，但按"成熟量化交易系统"的标准衡量，它当前不适用于无人值守的真实资金运行。** 核心原因不是策略本身，而是执行可靠性存在 6 个可定位到代码行的真实缺陷：自动驾驶的止损完全依赖本地进程、切换标的/模式后旧持仓实际失去管理、保护价单不成交会产生"幽灵持仓"、对账循环会把模拟盘挂单误杀、熔断手动恢复后立即复熔断。

分项评级（A 优秀 / B 良好 / C 及格 / D 不合格）：

| 维度 | 评级 | 一句话结论 |
| --- | --- | --- |
| 架构与代码质量 | **B+** | 模块边界清晰、单一管道、fail-closed 哲学贯穿全链路 |
| 交易所接入 | **B** | 手写 OKX 客户端质量好（重连/心跳/REST 兜底/幂等/时间同步），仅支持 OKX |
| 风控体系 | **B-** | 事前/事中/事后三层齐全且持久化，但存在 2 个可被极端行情利用的漏斗 |
| 执行可靠性（OMS/autopilot） | **C** | 设计好，但有 4 个会在真实行情中触发的状态一致性缺陷 |
| 回测与研究体系 | **B+** | 回测与实盘同源决策链、walk-forward/holdout/Monte Carlo 齐备，优于多数同类 |
| 策略生态 | **C-** | 仅 2 个模板策略 + 1 套内置规则引擎，无插件化，因子硬编码 |
| 安全与鉴权 | **B** | fail-closed + AES-GCM + 二次确认，但 `/ws` 完全无鉴权、CORS 全开 |
| 运维可观测性 | **D** | 无告警外发通道、无指标监控、无 CI、无容器化、日志仅 stdout |
| 适用性判断 | — | **paper / OKX 模拟盘：可靠可用。实盘：仅适合小资金 + 有人盯盘；无人值守实盘当前不可靠** |

---

## 二、系统概览

### 2.1 定位与形态

- 单进程 Python 3.12 + FastAPI 应用，本地部署（默认只监听 `127.0.0.1:8000`），React 18 前端由后端同源静态托管，另有移动端 H5。
- 通过 OKX API v5（REST + WebSocket 三通道）接入行情与交易，支持全量 USDT 现货 + USDT 永续作为"支持集"。
- 两级交易引擎：**本地模拟盘（PaperEngine，虚拟资金撮合）** 与 **实盘/模拟盘 OMS（对接 OKX）**，由系统级 `venue` 开关路由；自动驾驶（Autopilot，纯规则）跟随 venue 执行。
- 数据落 SQLite（WAL 模式），行情以"当前驾驶标的"为中心做 WS 订阅 + REST 兜底。

### 2.2 核心链路

```
OKX WS(business candle, confirm=1 收盘确认) / REST 兜底
   → DataService（bar/tick 事件）
   → Autopilot._bar_loop（bar 收盘决策，_deciding 重入守卫）
       因子引擎(8 因子+七态 regime) → 双方向 detect_setup → score(≥70)
       → 11 项守卫 → plan_trade(结构止损+RR) → position_size(1% 风险预算)
       → 风控降档(normal/reduced/minimum/paused)
   → executor 派发（paper: PaperEngine / okx: OMS.place_intent）
       → validate_intent → risk.check_pretrade（_place_lock 原子）
       → 市价转 IOC±0.2% 保护价 → OKX 下单（clOrdId 幂等）
   → tick 循环本地管理持仓（SL/TP1 25%保本/TP2 35%/ATR trailing/3R/时间止损）
   → 对账循环(60s) + 私有 WS 订单回报 + 断线重同步
```

风控独立成层：`RiskEngine.check_pretrade`（支持集白名单/熔断/杠杆上限/频率/单笔限额/仓位占比）+ 事中权益监控（日亏/回撤自动熔断、venue 分账基线）+ kill switch（撤单+全平+停所有策略）+ 熔断状态持久化（重启不可绕过）。

---

## 三、做得好的地方（对标后确认的亮点）

这些是对照成熟系统后确认达到甚至优于同类实践的部分，评估短板时应一并看到：

1. **fail-closed 贯穿全链路**：未配置 API Token 时交易接口全部 401（`api/auth.py:54-67`）；无法确认 OKX 账户模式时禁止合约开仓（`oms.py:99-104`）；set_leverage 失败即禁止开仓（`oms.py:108-114`，防止"按错误杠杆下单"）；私有 WS 不健康禁止实盘开新仓（`autopilot.py:197-205`）；K 线数据陈旧超过 2 个周期直接停止决策（`autopilot.py:412-420`）；regime=unknown（数据不足/含 NaN/非正价格）禁止开仓（`factor_engine.py:210-220`、`signal_engine.py:412-414`）。这套"不确定就不交易"的纪律与 Nautilus Trader 等系统的风险哲学一致，明显优于常见业余实现。
2. **执行保护符合行业标准**：裸市价单一律转限价 IOC + ±0.2% 保护价（红线 R2，`oms.py:66-73`），与成熟系统"防穿单/防滑点失控"的做法一致；`clOrdId` 幂等 + 网络超时重试依赖交易所去重；60 秒对账循环兜底孤儿单（`oms.py:412-436`）；私有 WS 重连后主动 `sync_all`（`oms.py:392-410`）。
3. **风控有"重启不可绕过"设计**：熔断状态（`halted`）持久化在 DB settings（`risk_engine.py:233-235`）；基线按 venue 严格分账（`risk_engine.py:138-161`），避免 paper 1 万初始资金污染 OKX 空账户导致 100% 假回撤误熔断——这是踩过坑之后才有的设计。
4. **回测-实盘同源**：`rules_backtest.py` 复用 `factor_engine` + `signal_engine`（同一套评分/守卫/计划代码），信号 bar i 收盘产生、bar i+1 开盘价 ±滑点成交，明确的无未来函数纪律（HTF 只用已收盘 bar、trailing 本根更新下根生效、同 bar 双触发按止损计），并自带 Monte Carlo 重排检验"顺序幸运"（`engine.py:261-298`）。配套 walk-forward、holdout、OOS 断点检验脚本，以及一份写得相当严肃的防过拟合研究纪律文档（BITVAULT_STRATEGY_RESEARCH.md：样本量门槛、时序切分、禁止自动转正）。
5. **全链路可审计**：每次决策（含观望及卡在哪一层）写 `signals`；每笔平仓写 `trade_journal`（MFE/MAE/守卫快照/regime/setup/风险档位）；所有写操作有 `audit_logs`；风控事件独立成表。可解释性达到 freqtrade 以上的水准。
6. **数据链路自愈**：公共/business 双 WS 分端点，business 通道有"业务心跳"（300 秒无 candle 强制重连自愈，`data_service.py:397-413`），REST 全兜底且 REST 数据不会伪造业务心跳（`via_rest` 区分，防"自我救活"误判）；bar 以 confirm=1 收盘确认入库，无 repaint。
7. **密钥与鉴权**：API Key AES-256-GCM 加密落库、主密钥本地文件、永不入库不入仓；高风险接口要求 `confirm:true` 二次确认；实盘策略实例重启后强制 `pending_confirm` 人工恢复（红线 R7）。
8. **测试**：56 个单元测试 1.3 秒全部通过（本次评估实际运行验证），覆盖风控引擎、精度回归（微价币 tickSz/lotSz）、venue 隔离、reduce-only 语义、WS 心跳等关键路径。

---

## 四、主要短板：已核实的问题清单

以下问题均已在代码中逐行核实，按严重程度排序。**P0 = 涉及真实资金安全，实盘前必须修复；P1 = 高概率造成资损/功能失效或安全暴露；P2 = 质量/运维问题。**

### P0-1 自动驾驶持仓没有任何交易所侧止损，进程宕机 = 止损消失

- **位置**：`autopilot.py:771-776`（开仓 `place_intent` 不传 `sl_trigger_px/tp_trigger_px`）；对照 `oms.py:130-139`（OMS 明明支持 `attachAlgoOrds` 交易所侧止损止盈，双均线模板策略在用，红线 R6）。
- **场景**：autopilot 的止损/止盈/trailing/时间止损全部在本地 tick 循环执行（README 已声明是设计如此）。进程崩溃、断电、部署重启的任何时刻，交易所侧没有任何挂单保护。
- **影响**：极端行情下从进程死亡到人工发现之间，持仓完全裸奔。这是与成熟系统最大的单点差距——freqtrade 提供 `stoploss_on_exchange` 正是为此。
- **加重因素**：重启后 `_discover_position`（`autopilot.py:243-302`）用**当前因子重算** SL/TP 收养持仓：若重启前已触发 TP1（止损已推保本），重启后保本状态丢失，止损可能被放回比原计划更差的位置；`tp1_done/tp2_done` 分批进度也一并丢失。
- **建议**：autopilot 开仓时附带交易所侧 SL（`slTriggerPx` 市价止损已支持），本地 trailing 只做收紧方向的更新并同步改单；收养持仓时 SL 只允许向保护方向移动（多头取 max、空头取 min），持久化 `tp1_done/tp2_done`。

### P0-2 切换驾驶标的后，旧标的持仓的本地管理实际上完全失效

- **位置**：tick/bar 事件只对 `active_inst` 发布——`data_service.py:302-314`（公共频道与 candle 频道都只订阅 `self.active_inst`），`_load_all_tickers`（`data_service.py:129-163`）刷新全量价格但不发 tick 事件。
- **场景**：`on_instrument_switch` 的注释与通知宣称"旧标的已托管持仓继续由 tick/bar 循环止损止盈"（`autopilot.py:356-366, 1061-1081`），但事件路由按 `inst_id` 分发的前提是该标的还有事件流——切换后旧标的一根 bar、一个 tick 都不会再来。
- **影响**：旧持仓的 0.4%~2.5% 级别位置级止损永远不会触发，直到用户切回该标的。此时唯一兜底是账户级风控（10 秒轮询 totalEq → 默认 3% 日亏熔断 → 全平），保护粒度粗了一个数量级。
- **建议**：持仓中标的必须保留最小数据流（对持仓标的追加 tickers 订阅即可，成本极低），或切换标的存在未平仓持仓时阻止/警告。

### P0-3 venue 切到 paper 时，实盘持仓脱离本地管理

- **位置**：`on_venue_switch` 清空 `self.positions`（`autopilot.py:1026-1059`），文档注明"旧 venue 持仓不在此托管，切回时再收养"。
- **场景**：用户在 OKX 实盘有持仓，把顶栏切到 paper（一个随手操作，无任何拦截），实盘持仓立即失去本地止损（叠加 P0-1 也没有交易所侧止损）。
- **影响**：与 P0-2 同根因，但发生在真实资金上，且触发只需一次误点击。账户级熔断是唯一兜底。
- **建议**：`set_venue('paper')` 时若 OKX 侧存在非空持仓，应拒绝或强制二次确认并提示风险。

### P0-4 保护价 IOC 未成交会产生"幽灵持仓"，autopilot 卡死

- **位置**：`autopilot.py:771-816`。开仓后不校验订单状态/成交回报，直接以 `last_px` 记录 `self.position`；`_close_position`（`autopilot.py:869-901`）按本地记录数量发 reduce-only 单并无条件扣减本地 `pos["sz"]`。
- **场景**：市价单转 IOC±0.2% 保护价（`oms.py:66-73`），快速行情中价格瞬间越过保护价 → IOC 全额不成交 → OKX 稍后回报 `canceled`，但 autopilot 已经记录了一个不存在的持仓。部分成交时本地账本也会按全量扣减，与交易所漂移。
- **影响**：a) 幽灵持仓阻止后续所有开仓（`has_position`）；b) 对幽灵仓的平仓单被交易所拒绝（无仓可减），tick 循环每个 tick 重试报错，系统进入死锁直到人工介入；c) 止损"已触发"的假象下实际没有任何风险敞口对冲动作与真实仓位对应。
- **建议**：开仓后等待订单终态（或读 `filled_sz`）再回填 entry/sz，未成交视为放弃并清位；平仓失败达到重试上限时应触发重同步而非无限重试。

### P0-5 OMS 对账循环缺 venue 过滤，会误杀模拟盘挂单

- **位置**：`oms.py:416-418` 的 `_reconcile_loop` 查询 `SELECT * FROM orders WHERE state IN (...)` **没有** `venue='okx'` 条件；对照同文件 `sync_all`（`oms.py:398-399`）有正确过滤。
- **场景**：连接任意 API Key（含 demo）后 OMS 启动，60 秒内对账循环把 paper 限价挂单（clOrdId 前缀 `PP`）拿去 OKX 查询 → 查无此单（51601）→ 本地标记 `failed`。
- **影响**：只要连了 Key，paper 网格/限价类策略的挂单会批量被"处决"，模拟盘验证结果失真——而模拟盘正是上线实盘前的主要验证手段。
- **建议**：一行修复，补 `venue='okx'`；并补一条"连接 Key 后 paper 挂单不受影响"的回归测试。

### P0-6 人工解除熔断后，下一个权益事件立即再次熔断

- **位置**：`risk_engine.py:282`（`resume()` 重置的是旧全局键 `day_start_equity`）；而 `on_equity` 读取的是 venue 分账键 `okx_day_start_equity` / `paper_day_start_equity`（`risk_engine.py:148-152, 184-188`）。
- **场景**：日亏 3% 触发熔断 → 用户确认后点"恢复" → 下一次账户刷新（≤10 秒）→ `(day_start - equity)/day_start` 仍 ≥3% → 立即再次熔断。
- **影响**：熔断恢复功能在"当日亏损型熔断"场景下事实上不可用（除非等 UTC 日切或手改数据库）。平仓后的权益下降同样计入，意味着真正需要它的场景几乎必然复熔断。
- **建议**：`resume()` 应重置当前 venue 的 `day_start_equity`（与代码注释声明的意图一致），并顺带修复同函数中的 venue 无关问题。

### P1-1 权益单次骤降 >20% 一律按"出入金"重置基线，极端行情漏熔断

- **位置**：`risk_engine.py:153-161`。
- **场景**：10 秒轮询间隔内权益跌超 20%（插针、连环爆仓、多仓叠加）会被判定为"大额出金"→ **重置峰值与日亏基线** → 本次暴跌完全不参与日亏/回撤熔断计算，只有"之后的继续下跌"才计入。
- **建议**：用 OKX 资金流水（bills）或账单变更事件区分真实出入金与市场亏损；无法区分时保守双轨（既重置基线也记录一次 warning 级回撤事件）。

### P1-2 `/ws` 完全无鉴权，反代部署时泄露全部交易事件流

- **位置**：`api/ws.py:16-40`；鉴权中间件只覆盖 `/api` 前缀（`api/auth.py:85-96`）。
- **影响**：本地绑定 127.0.0.1 时风险有限；但 README 的生产形态是"反向代理 + HTTPS 对外"，此时任何访客都能连 `/ws` 拿到订单、账户、风控、决策日志全量实时流（含标的、方向、数量、价格、权益），Bearer Token 体系被整体绕过。
- **建议**：WS 握手阶段校验 token（query 参数或首条消息认证），未认证只允许公开主题。

### P1-3 风控日切用 UTC，策略节流用本地时间，口径分裂

- **位置**：`risk_engine.py:149`（`time.gmtime`）vs `autopilot.py:666`（`time.strftime` 本地）。
- **影响**：日亏熔断基线在 UTC 8:00（北京时间）重置，而"当日开仓数/当日已实现盈亏"在本地零点重置；两者组合出的"当日风险窗口"不对应任何一个自然日，边界行为难以推理与复盘。
- **建议**：统一为可配置时区的自然日，并在风控事件中记录所用口径。

### P1-4 Kill Switch / 自动熔断不处理现货持仓

- **位置**：`oms.py:351-363` 只遍历 `account.positions`，而 OKX `/account/positions` 不含现货余额（`account_service.py:90-103`）。
- **影响**：熔断"全平"只平合约；策略买入的现货会留在账户里继续承担行情风险（风控语义与用户预期不符）。
- **建议**：全平时对支持集内现货余额一并市价卖出，或明确在 UI/文档声明只覆盖合约。

### P1-5 节流/连亏/当日盈亏全是内存态，重启即清零

- **位置**：`autopilot.py:161-168`（`_daily_opens/_loss_streak/_sleep_until/_daily_realized_pnl/_paper_peak_equity`）。
- **影响**：`max_opens_per_day`、连亏强制休眠、当日亏损降档（paused 档）都可以通过重启进程绕过；策略级回撤峰值的内存态在重启后从当前权益重新起算。熔断本身有持久化，但策略层的"软风控"没有。
- **建议**：这些计数器低成本持久化到 settings（每次开/平时写一次即可）。

### P1-6 `unknown` 状态订单永远不再对账

- **位置**：`oms.py:210-216` 引入 `unknown` 兜底状态，但 `OPEN_STATES` 不含它，对账循环只处理三种 open 状态（`oms.py:412-431`）。
- **影响**：收到未识别 OKX 状态的订单会永久停留在 unknown，前端与对账都不再管它。
- **建议**：unknown 也纳入对账拉终态。

### P1-7 CORS 全开 + 公开 GET 通知接口泄露交易活动

- **位置**：`main.py:329-331`（`allow_origins=["*"]`）；`api/auth.py:29`（`/api/notifications` 在公开只读名单，内容含"自动驾驶开多 XXX @ 价格"等）。
- **影响**：用户浏览器打开任意网页时，该网页的 JS 可跨域读取本机 8000 端口的通知/状态接口（drive-by 探测），暴露持仓方向与交易节奏。写操作因 token+confirm 仍安全，但隐私面较大。
- **建议**：CORS 收敛到前端来源（同源部署时可直接去掉 CORS 中间件）；通知移出公开名单。

### P2（择要）

| # | 问题 | 位置 | 说明 |
| --- | --- | --- | --- |
| P2-1 | 交易日志平仓价用 `last_price` 而非实际成交价 | `autopilot.py:977` | MFE/MAE/pnl 归因数据与真实成交有偏差，影响后续统计可信度 |
| P2-2 | SQLite 单连接同步 IO 跑在事件循环里，每写必 commit | `db.py:219-251` | 个人规模可用；行情高频写入时阻塞循环，且无归档/清理策略（`signals`/`bars` 无限增长） |
| P2-3 | 回测不模拟资金费率、滑点为固定比例、现货网格允许负仓位 | `engine.py:345-355` | 系统 `caveats` 已诚实声明；SWAP 回测收益系统性偏乐观 |
| P2-4 | RSI 用简单均值（Cutler 变体）而非 Wilder 平滑；EMA/MACD 种子方式自定 | `factor_engine.py:27-63` | 内部自洽（回测实盘同源），但与 TradingView/交易所指标数值不可直接对比 |
| P2-5 | `position_size` 说明文案 `1%%` 显示错误 | `signal_engine.py:534` | 纯展示问题 |
| P2-6 | 支持集 = 全量 USDT 现货+永续（数百个），未按流动性分层 | `data_service.py:50-97` | 精度处理有回归测试兜底（微价币），但小币滑点模型不可信，建议默认白名单头部高流动性标的 |
| P2-7 | `risk.status()` 返回旧键 `day_key`/`day_start_equity`（恒空/旧值） | `risk_engine.py:212-220` | 前端展示口径与实际 venue 分账键不一致 |
| P2-8 | paper 限价撮合不检查跳空（缺口时仍按限价成交）与成交量 | `paper_engine.py:144-167` | 保守方向可接受，但会高估成交概率 |
| P2-9 | 前端无任何测试；仓库无 CI、无 lint/format/类型检查配置、无容器化 | 仓库根 | 工程化短板，见下节 |

---

## 五、与成熟系统的对标分析

选取四个有代表性的参照系：**freqtrade**（最流行的开源加密交易机器人，趋势/套利通用）、**Hummingbot**（做市/撮合导向）、**Nautilus Trader**（机构级事件驱动框架）、**Jesse**（研究型回测框架），以及商业托管产品（3Commas/Cryptohopper 一类）作为产品化参照。

| 维度 | BitVault 现状 | 成熟系统通行水平 | 差距评价 |
| --- | --- | --- | --- |
| 交易所支持 | 仅 OKX，手写 REST+WS 客户端 | freqtrade 走 CCXT 支持 30+ 交易所；Hummingbot 有连接器框架 | **数量级差距**。但 BitVault 单所客户端质量不低（限频/退避/时间同步/代理/幂等齐全），单所定位下可接受 |
| 订单执行 | 单一管道、IOC 保护价、clOrdId 幂等、60s 对账、重连重同步 | 同等能力是标配；另普遍具备：部分成交精细处理、订单状态机持久化完整、交易所侧止损可选（`stoploss_on_exchange`）、做市报价引擎 | **核心差距在 P0-1/P0-4**：本地状态与交易所状态的强一致保证不足 |
| 风控 | 事前 6 项 + 权益熔断（venue 分账、持久化）+ kill switch + 策略级四档降档 + 禁马丁 | freqtrade protections（Cooldown/StoplossGuard/MaxDrawdown）；商业产品普遍只有日亏停机 | **功能面达到甚至超过开源平均**，但 P1-1（骤降误判出金）与 P0-3（切 venue 脱管）是成熟系统不会有的漏洞 |
| 回测/研究 | 与实盘同源决策链、下根 bar 成交、Monte Carlo、walk-forward/holdout 脚本、防过拟合纪律文档 | freqtrade 回测+hyperopt；Jesse 多周期精确回测；Nautilus 纳秒级回放 | **研究纪律是亮点**，超多数同类；工具链自动化程度低于 hyperopt（无参数优化搜索），回测成本模型缺资金费率 |
| 数据 | 单标的实时订阅 + REST 兜底 + 收盘确认 + 本地落库 + 异常数据清洗脚本 | 多标的并行、历史数据管理、数据完整性校验、tick 级数据 | 单标的中心架构直接导致了 P0-2；个人定位下够用 |
| 策略生态 | 2 个模板（双均线/网格）+ 1 套内置规则引擎（8 因子、阈值硬编码，仅研究脚本可调） | freqtrade 声明式策略接口 + hyperopt + 海量社区策略；商业产品有信号市场 | **最大短板之一**。因子/阈值全部硬编码在 `signal_engine.py`，无插件化接口，策略迭代要改核心代码 |
| 告警与远程运维 | 无（仅站内通知表 + 前端推送） | freqtrade 内置 Telegram 控制（查仓/平仓/开关机）；商业产品 App 推送 | **明显短板**：无人值守场景下"出事不知道"是风控体系之外的又一层裸奔 |
| 工程化 | 56 个单测通过、无 CI、无 lint/类型检查、无容器、日志 stdout、部署脚本已删除 | 主流项目均有 CI 矩阵、镜像发布、结构化日志 | 差距明显，但对单人项目属常见取舍 |
| 安全 | fail-closed token、confirm 二次确认、AES-GCM 密钥、实盘重启需人工恢复 | 商业托管：OAuth/2FA；自托管类同 | 达到自托管同类水准；`/ws` 无鉴权（P1-2）是例外 |

**对标结论**：BitVault 在"风控设计意识、决策可审计性、回测研究纪律"三项上高于大多数开源同类；在"执行状态一致性、策略生态、告警运维、工程化"四项上低于成熟系统。它目前的形态更像一个**优秀的个人交易终端 + 研究工作台**，而成熟系统是"可在无人监督下长期运行的执行机器"——两者差距集中在第四节 P0/P1 清单。

---

## 六、分场景可靠性结论

| 场景 | 结论 |
| --- | --- |
| 纯行情查看 / forecast 参考 / 决策流围观 | **可靠**。数据链路有自愈与兜底，无资金风险 |
| PaperEngine 模拟盘验证策略 | **可靠**（修掉 P0-5 前不要同时连接 API Key，否则挂单会被误杀） |
| OKX 模拟盘（demo）跑 autopilot | **基本可靠**，幽灵持仓（P0-4）在对/demo 资金无实害，但会卡住决策，需要知道这个失败模式 |
| 实盘 + 有人在电脑前盯盘 + 小资金 | **有条件可用**：P0 清单中的问题都可被人工及时兜住，风控三层 + kill switch 提供了体面的手动兜底 |
| 实盘 + 无人值守（过夜/挂机） | **当前不可靠**：P0-1（无交易所侧止损）+ P0-2/P0-3（切标的后脱管）+ P1-5（软风控重启清零）+ 无告警外发，任何一环出问题都无人知晓 |

---

## 七、改进路线图（按优先级）

### 第一批：实盘前置条件（预计工作量小，收益最大）

1. autopilot 开仓附带交易所侧止损（`attachAlgoOrds` 已支持），本地管理只做收紧与分批；收养持仓时止损只许向保护方向移动（P0-1）。
2. 持仓标的保留 tickers 订阅，修复切换标的后的脱管；`set_venue('paper')` 前校验实盘持仓（P0-2/P0-3）。
3. 开仓后按订单终态/`filled_sz` 回填持仓，未成交清位；平仓重试加上限与重同步（P0-4）。
4. `_reconcile_loop` 补 `venue='okx'` 过滤 + 回归测试（P0-5，一行）。
5. `resume()` 重置 venue 分账的日亏基线（P0-6）。

### 第二批：短期加固（1~2 周）

6. `/ws` 握手鉴权；CORS 收敛；通知移出公开名单（P1-2/P1-7）。
7. 出入金改用 bills 流水判定，替代"跌 20% 即出金"启发式（P1-1）。
8. 统一日切口径；unknown 订单纳入对账；kill switch 覆盖现货（P1-3/P1-6/P1-4）。
9. 接入 Telegram Bot / Server酱 / webhook 告警：熔断、kill switch、连亏休眠、数据陈旧、WS 断流必须外发。
10. 节流/连亏/当日盈亏/回撤峰值持久化（P1-5）。

### 第三批：中期工程化

11. GitHub Actions CI（pytest + ruff + mypy 渐进引入）；Dockerfile 与 compose。
12. 决策参数外置为配置文件/DB（score_min、ATR 倍数、TP 分批等目前硬编码），为后续参数优化铺路。
13. 回测补资金费率模型；paper 撮合补跳空检查。
14. 结构化日志 + 简单指标端点（决策延迟、WS 健康度、下单失败率）。

### 第四批：长期演进

15. 策略/因子插件化接口（参照 freqtrade 的 IStrategy 契约），把 `signal_engine` 从"唯一策略"降级为"内置策略之一"。
16. 若确有多所需求，引入 CCXT 或连接器抽象层，避免手写客户端逐一适配。
17. 数据层在 bars 超过千万行时迁移时序库；signals 表增加保留策略。

---

## 八、结语

这份代码库最难得的是**纪律**：所有下单必须过同一根管道、所有不确定态都导向"不交易"、所有决策留痕可复盘、回测与实盘共享同一套规则代码、研究流程写明禁止过拟合。这些是多数个人量化项目做不到、而成熟系统赖以生存的骨架。

但它目前的可靠性是"**进程活着、有人看着**"前提下的可靠性。量化系统真正区分业余与成熟的，恰恰是进程死了、网络断了、行情极端了之后剩余的保护还剩多少——BitVault 在这三问上的答案目前是"几乎没有剩余保护"。第一部分路线图中的六个 P0 修复（合计工作量估计在数天量级，其中两个是一行级修复）完成后，本系统的定位可以从"有人盯盘的小资金实盘终端"升级为"可短暂无人值守的个人实盘系统"；再补齐告警外发与工程化之后，才谈得上与 freqtrade 级别的成熟系统在同一标准下对话。

---

*附：本报告所有问题定位均基于 2026-09-09 的 master 分支（commit b62fbce）；56 个单元测试在该时点全部通过。*
